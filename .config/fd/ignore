# Global fd ignore (gitignore syntax). Applies everywhere, including $HOME.
.git/
node_modules/
.cache/
.terraform/
.venv/
Library/
go/pkg/
# ~/dd symlinks to go/src/github.com/DataDog; hide the canonical copy so
# --follow surfaces the tree once (via ~/dd) instead of twice.
go/src/

# Language toolchains / package caches
.pyenv/
.rbenv/
.cargo/
.rustup/
.npm/

# Cloud sync, backups, and media (slow to stat or irrelevant to code search)
Google Drive/
.Datadog-Configuration-Backups/
.local/
Pictures/
Movies/
Music/
.Trash/
